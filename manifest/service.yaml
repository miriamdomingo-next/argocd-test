apiVersion: v1
kind: Service
metadata:
  name: k8s-ip-app1
spec:
  type: NodePort
  selector:
    app: ip-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080  # You can change this if nee
